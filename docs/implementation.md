# Implementation tracker

Preserve React/Vite, Express and Mongoose. Implement validated environment and authentication, transactional bookings, verified idempotent payments, individual QR tickets, complete role APIs and workspaces, discovery, jobs, uploads, and integration tests. External services require operator credentials. Never silently simulate a payment, upload, refund or payout. Never seed an existing database automatically.
